# Codex Agents

This directory is reserved for Codex-oriented agent prompts or future mirrors of portable agent definitions.

- Keep `AGENTS.md` as the primary repository instruction surface.
- Keep portable skills authored in `.agents/skills/` first.
- Add files here only when they are truly Codex-specific or when a Codex loader expects an `agents/` path.
